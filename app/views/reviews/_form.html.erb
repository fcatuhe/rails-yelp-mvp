<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: (0..5).to_a, as: :radio_buttons %>
  <%= f.submit "Add this review", class: "btn btn-primary" %>
<% end %>
